import { registerRootComponent } from 'expo';
import App from './src/App';

console.log('🚀 [STARTUP] index.js - Entry point loaded');

registerRootComponent(App);

console.log('🚀 [STARTUP] index.js - App registered as root component');

